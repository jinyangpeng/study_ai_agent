"""CLI commands - console-script entry points.

After `pip install -e .` these are exposed as:

    dev   -> run the dev server
    prod  -> run the prod server
    lint  -> run ruff check
    fmt   -> run ruff format
"""
